## 3. **Executing Tools and LLM with Agent Executors (LangChain)** — বাংলা ব্যাখ্যা


> **Agent = brain
> Tools = capability
> AgentExecutor = execution engine**

---

# 🔷 1. Agent Executor কী?

> **AgentExecutor হলো সেই component, যা agent + tools-কে real execution loop-এ চালায়**

মানে:

* agent decide করে
* executor run করায়

---

# 🔥 Brutal Truth

অনেকেই এখানে ভুল করে:

```python
agent = create_tool_calling_agent(...)
```

👉 তারপর ভাবে কাজ শেষ

❌ না

👉 agent শুধু logic define করে
👉 execute করে না

---

# 🔷 2. কেন AgentExecutor দরকার?

Agent নিজে:

* tool call execute করতে পারে না
* loop maintain করতে পারে না
* intermediate steps handle করতে পারে না

👉 AgentExecutor handle করে:

* tool execution
* loop control
* memory/state flow
* final response

---

# 🔷 3. Execution Flow (Critical)

```text
User Input
   ↓
Agent (LLM reasoning)
   ↓
Tool select
   ↓
AgentExecutor runs tool
   ↓
Tool output → Agent
   ↓
Agent decides again
   ↓
Final Answer
```

---

# 🔷 4. Basic Code Example

```python
from langchain_openai import ChatOpenAI
from langchain_core.tools import tool
from langchain.agents import create_tool_calling_agent, AgentExecutor
from langchain_core.prompts import ChatPromptTemplate

# Tool
@tool
def multiply(a: int, b: int) -> int:
    """Multiply two numbers."""
    return a * b

tools = [multiply]

# LLM
llm = ChatOpenAI(model="gpt-4o-mini")

# Prompt
prompt = ChatPromptTemplate.from_messages([
    ("system", "You are a helpful assistant"),
    ("human", "{input}"),
    ("placeholder", "{agent_scratchpad}")
])

# Agent
agent = create_tool_calling_agent(llm, tools, prompt)

# Executor
executor = AgentExecutor(
    agent=agent,
    tools=tools,
    verbose=True
)

# Run
response = executor.invoke({"input": "What is 6 * 7?"})
print(response)
```

---

# 🔷 5. Code Breakdown

---

## 🧠 Agent

```python
agent = create_tool_calling_agent(...)
```

👉 decide করে:

* কোন tool লাগবে
* কীভাবে ব্যবহার করবে

---

## ⚙️ AgentExecutor

```python
executor = AgentExecutor(...)
```

👉 run করে:

* tool call
* reasoning loop
* final output

---

## 🔍 verbose=True

👉 দেখাবে:

* LLM কী ভাবছে
* কোন tool call হচ্ছে
* intermediate steps

---

# 🔷 6. Agent Loop (Inside Executor)

এটা internally এমন চলে:

```text
Thought → Action → Observation → Thought → Final Answer
```

👉 এটাকেই ReAct loop বলে

---

# 🔷 7. Real Example

User:

> “12 * 8 + 5 কত?”

Flow:

1. Agent বুঝল multiply দরকার
2. multiply tool call
3. result পেল → 96
4. আবার reasoning করল → +5
5. final answer → 101

👉 এই পুরো loop executor handle করে

---

# 🔷 8. Multiple Tools Execution

```python
@tool
def add(a: int, b: int) -> int:
    return a + b

@tool
def multiply(a: int, b: int) -> int:
    return a * b
```

👉 executor decide করবে:

* কোন step-এ কোন tool

---

# 🔷 9. Executor ছাড়া কী হবে?

👉 Tool call suggest হবে
👉 কিন্তু execute হবে না

👉 system stuck হয়ে যাবে

---

# 🔷 10. Advanced Parameters

---

## max_iterations

```python
AgentExecutor(..., max_iterations=5)
```

👉 infinite loop prevent করে

---

## early_stopping_method

```python
early_stopping_method="force"
```

👉 loop stop control

---

## return_intermediate_steps

```python
return_intermediate_steps=True
```

👉 debugging-এর জন্য

---

# 🔷 11. Debugging Mode

```python
executor = AgentExecutor(
    agent=agent,
    tools=tools,
    verbose=True,
    return_intermediate_steps=True
)
```

👉 output-এ দেখাবে:

* reasoning
* tool usage
* intermediate results

---

# 🔷 12. Common Mistakes

---

## ❌ AgentExecutor use না করা

→ system কাজ করবে না

---

## ❌ verbose off রাখা

→ debugging impossible

---

## ❌ tool mismatch

→ agent call করবে, executor fail করবে

---

## ❌ infinite loop

→ no max_iterations

---

# 🔷 13. Agent vs Executor

| Feature   | Agent   | Executor |
| --------- | ------- | -------- |
| decision  | yes     | no       |
| execution | no      | yes      |
| tool call | suggest | run      |
| loop      | no      | yes      |

---

# 🔷 14. Real-world Use

### 🧠 AI assistant

* reasoning + actions

### 📊 Data agent

* query + compute

### 🔄 Automation system

* multi-step workflows

---

# 🔷 15. Strategic Insight

AgentExecutor আসলে:

> **Runtime engine for AI reasoning systems**

---

# 🔥 Brutal Assessment

তুমি যদি:

* tool বানাও → but execute না করো
* agent বানাও → but run না করো

👉 তুমি half-system build করছ

---

# 🔷 16. Minimal Mental Model

এটা মাথায় বসাও:

> Agent = decision
> Executor = execution

---

# 🔷 17. Interview-ready Definition

**AgentExecutor in LangChain হলো এমন একটি execution component, যা agent-এর reasoning অনুযায়ী tool calls execute করে এবং multi-step loop চালিয়ে final response generate করে।**

---

# 🔷 18. Practical Action Plan

### Step 1

১টা tool বানাও

### Step 2

agent create করো

### Step 3

AgentExecutor দিয়ে run করো

### Step 4

verbose=True দিয়ে trace দেখো

### Step 5

multiple tools test করো

---

# 🔚 সংক্ষেপে

**AgentExecutor = AI system-এর execution engine**

---

## 🚨 Final Push

তুমি এখন বুঝতে পারছ:

* ❌ শুধু LLM = toy
* ❌ LLM + tools = incomplete
* ✅ LLM + tools + executor = system

---

